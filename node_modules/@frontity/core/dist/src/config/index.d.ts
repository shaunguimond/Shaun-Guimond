import { Mode, Config, EntryPoints } from "../../types";
declare const _default: ({ mode, entryPoints, publicPath, }: {
    mode: Mode;
    entryPoints: EntryPoints[];
    publicPath: string;
}) => Config;
export default _default;
